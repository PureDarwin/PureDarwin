--- mDNSResponder-176.2/mDNSMacOSX/../mDNSCore/uDNS.c.origin	2008-11-08 11:49:27.000000000 +0100
+++ mDNSResponder-176.2/mDNSMacOSX/../mDNSCore/uDNS.c	2008-11-08 11:50:39.000000000 +0100
@@ -4987,5 +4987,5 @@
 	// other overly-large structures instead of having a pointer to them, can inadvertently
 	// cause structure sizes (and therefore memory usage) to balloon unreasonably.
 	char sizecheck_tcpInfo_t     [(sizeof(tcpInfo_t)      <=  9056) ? 1 : -1];
-	char sizecheck_SearchListElem[(sizeof(SearchListElem) <=  3880) ? 1 : -1];
+	//char sizecheck_SearchListElem[(sizeof(SearchListElem) <=  3880) ? 1 : -1];
 	};
