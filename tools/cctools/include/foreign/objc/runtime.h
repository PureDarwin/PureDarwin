/* Stub: real objc/runtime.h isn't available on non-Darwin hosts, and none
 * of our host-otool build's actual compiled sources use any real ObjC
 * runtime symbols (only unconditionally #include this header defensively).
 */
