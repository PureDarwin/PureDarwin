# Builds run as the _nixbld users, unsandboxed: PureDarwin has no Seatbelt.
build-users-group = nixbld
sandbox = false
# cache.nixos.org serves macOS binaries under the same system name.
substituters =
experimental-features = nix-command flakes
