/*
 * LocalAuthentication's private constants. Everything Security actually names
 * from LocalAuthentication is declared in our coreauthd_spi.h, so this only has
 * to exist for the include to resolve.
 */
#ifndef PD_LAPRIVATEDEFINES_H
#define PD_LAPRIVATEDEFINES_H

#include <coreauthd_spi.h>

#endif /* PD_LAPRIVATEDEFINES_H */
