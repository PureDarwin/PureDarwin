buildscript {
    apply from: 'repo.gradle'
    repositories {
        maven { url harissa.build }
    }

    dependencies {
        classpath 'com.android.tools.build:gradle-experimental:0.10.0-alpha1'
    }
}

allprojects {
    repositories {
        maven { url harissa.test }
    }
}
