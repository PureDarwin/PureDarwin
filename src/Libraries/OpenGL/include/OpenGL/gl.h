/*
 * OpenGL.framework - <OpenGL/gl.h>.
 *
 * PureDarwin's GL comes from Mesa, whose headers are installed at
 * /usr/include/GL. Forward rather than vendor a second copy, so the framework
 * and a direct -I/usr/include GL user always agree.
 */

#ifndef _PUREDARWIN_OPENGL_GL_H
#define _PUREDARWIN_OPENGL_GL_H

#include <GL/gl.h>

#endif
